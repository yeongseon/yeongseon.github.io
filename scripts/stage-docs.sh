#!/usr/bin/env bash
set -euo pipefail

# Staging script placeholder.
#
# Future purpose: check out each tool repository, copy its `docs/` tree into
# `docs/azure-functions-python/<tool>/`, then let `mkdocs build` produce a
# single unified site. Kept intentionally empty until the first tool
# (OpenAPI) is integrated.

echo "stage-docs: not yet implemented" >&2
exit 0
